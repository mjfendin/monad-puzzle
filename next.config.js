/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "lqy3lriiybxcejon.public.blob.vercel-storage.com"
    ],
  },
};

module.exports = nextConfig;
